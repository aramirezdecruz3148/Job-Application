import formApi from '../src/form-api.js';
const test = QUnit.test;
QUnit.module('applicant api');



test('round-trip applicant data', (assert) => {
    // arrange
    // act
    // assert
    assert.deepEqual(result, applicant);
});